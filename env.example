# Database Configuration
# For Neon: Use the pooled connection string (with -pooler in hostname)
# Example: postgresql://user:password@ep-xxx-xxx-pooler.region.aws.neon.tech/dbname?connect_timeout=15&sslmode=require
DATABASE_URL="postgresql://user:password@localhost:5432/eyesoul_eyewear"
# Optional: Neon also provides POSTGRES_PRISMA_URL (can be same as DATABASE_URL)
POSTGRES_PRISMA_URL="postgresql://user:password@localhost:5432/eyesoul_eyewear"

# JWT Authentication
# REQUIRED in production. Must be at least 32 characters long.
# Generate a strong secret: openssl rand -base64 32
JWT_SECRET="replace-with-strong-secret-minimum-32-characters-long"

# NextAuth Configuration (optional, if using NextAuth)
NEXTAUTH_SECRET="replace-if-using-nextauth"
NEXTAUTH_URL="http://localhost:3000"

# Site URL (optional, for API calls and metadata)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Vercel Blob Storage (for file uploads)
# Get from Vercel Dashboard -> Storage -> Blob -> Settings
BLOB_READ_WRITE_TOKEN="your-vercel-blob-token"

# Rate Limiting with Upstash Redis
# Get from Upstash Dashboard: https://console.upstash.com/
UPSTASH_REDIS_REST_URL="your-upstash-redis-url"
UPSTASH_REDIS_REST_TOKEN="your-upstash-redis-token"

